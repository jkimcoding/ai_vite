{
  "name": "ai_vite",
  "main": "index.html",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "vite preview",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^16.5.0"
  }
}
